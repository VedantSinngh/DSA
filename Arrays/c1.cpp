#include <stdio.h>

int main() {
    int A[5] = {2, 4, 6, 8, 10};    // Array of 5 integers
    int B[5];   // Array of 5 integers with no initialization
    int C[10] = {2, 4, 6};    // Array of 10 integers
    int D[5] = {0};    // Array of 5 integers with all elements initialized to 0
    int E[] = {2, 4, 6, 8, 10};    // Array of 5 integers with size automatically calculated
    return 0;
}